pipeline {
    agent any

    stages {
        stage("Init") {
            steps {           
                echo "Initializing"
            }
        }
      
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
