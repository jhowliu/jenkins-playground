pipeline {
    agent any

    stages {
        stageg("Init") {
            steps {           
                echo "Initializing"
            }
        }

        stage("build") {
            steps {           
                echo "buidlddd"
            }
        }
      
    }
}
