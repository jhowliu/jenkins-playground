pipeline {
    agent any

    stages {
        stag("Init") {
            steps {           
                echo "Initializing"
            }
        }

        stage("build") {
            steps {           
                echo "buidlddd"
            }
        }
      
    }
}
